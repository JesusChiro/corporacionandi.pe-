{
  "extends": "astro/tsconfigs/strict",
  // Decorador para no poner .. en las importaciones 
  // de componentes
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "./src/*"
      ]
    }
  }
}